{
  "name": "stars-manager-for-me",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Stars 管理工具",
  "author": "Stars Manager",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev:desktop": "npm run build && electron .",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "prepare:electron": "node scripts/prepare-electron.cjs",
    "pack": "npm run build && npm run prepare:electron && electron-builder --win --config electron-builder.yml",
    "pack:dir": "npm run build && npm run prepare:electron && electron-builder --win --dir --config electron-builder.yml"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^34.2.0",
    "electron-builder": "^25.1.8"
  }
}
